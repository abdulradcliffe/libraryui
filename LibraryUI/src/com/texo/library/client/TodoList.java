package com.texo.library.client;

import com.google.gwt.core.client.EntryPoint;

public class TodoList /* implements EntryPoint */ {

	/* @Override */
	public void onModuleLoad() {

		/*
		 * Polymer.importHref(Arrays.asList( PaperIconItemElement.SRC,
		 * IronIconElement.SRC), new Function() { public Object call(Object arg) { //
		 * The app is executed when all imports succeed. startApplication(); return
		 * null; } });
		 */
	}

}
